query getApplications($groupId: String!) {
  group: groupByPk(id: $groupId) {
    groupApplications {
      application {
        id
        name
        topUrl
      }
    }
    apps {
      id
      name
    }
  }
}
