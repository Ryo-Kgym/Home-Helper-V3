query getApp($appId: String!) {
  app: appByPk(id: $appId) {
    __typename
    id
    name
    fields(orderBy: [{ index: ASC }]) {
      ...fragFields
    }
    records {
      ...fragRecords
    }
    linkDatabase {
      ...fragLinkDatabase
    }
  }
}
